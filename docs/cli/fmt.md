# catalyst fmt

```
Format project source files.


catalyst fmt [OPTIONS]


OPTIONS:
  -h,     --help              Print this help message and exit
  -p,     --profiles TEXT [[common]]  ...
                              Profile composition to build.
```

## Details

Expects a `.clang-format` file in the project root. It will recursively format all C/C++ files in the source directories.
