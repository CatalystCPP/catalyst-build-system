# catalyst completion

```
Generate shell completion scripts.


catalyst completion [OPTIONS] shell


POSITIONALS:
  shell TEXT:{bash,zsh} REQUIRED
                              The shell to generate completion for (bash, zsh)

OPTIONS:
  -h,     --help              Print this help message and exit
```

## Examples

```bash
catalyst completion
```
